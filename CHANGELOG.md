# 更新日志 / Changelog

本项目版本号遵循语义化版本。日期为发布日。

## 未发布 / Unreleased

证据体系重构：从「只给相似度分数」升级为「分层证据 + 合法共享剥离 + 校准后的证据强度」。围标判定不再依赖单一文本雷同，而是文本、取证、数值三类互相独立的证据并列举证；同时把「对招标文件的合法应答」从雷同统计中剥离，压掉最大的一类误报。

### 新增 · 取证硬证据 Features
- **同源痕迹取证**：docx 的 rsid 修订标识与包结构指纹、PDF 的 XMP GUID / trailer ID / 字体子集标签、跨文档的内嵌图片同源（精确 + 感知哈希）、共同错误指纹（词典外词 / 异常标点 / 引用错误）——这些痕迹改抬头、换措辞都洗不掉，与文本相似度完全正交。
- **逐字铁证**：跨文档极大公共子串检测，直接给出「甲第 3.2 节与乙第 3.2 节存在 800 字逐字相同」这类可写进评标报告的区间证据，带页码与章节定位。
- **对齐区段视图**：把散点雷同块合并成连续对齐区段（覆盖率 + 锚点数），新增双栏高亮屏——深红逐字、橙锚点、黄改写差异三级视觉。

### 新增 · 入口对抗 Features
- **规避特征检测**：零宽字符 / 双向控制符剥离、跨脚本同形字折叠（西里尔「а」冒充拉丁「a」）、PDF 隐藏文字层审计（不可见渲染 / 白字 / 出画布 / 极小字号）。
- **渲染-OCR 交叉验证**：抽样渲染页面做 OCR 与文字层比对，识别「渲染给评标人一套、抽取给查重系统另一套」的字体重映射与坐标乱序；命中后改用 OCR 文本参与比对并解除页数上限。

### 新增 · 合法共享剥离 Features
- **招标文件对减**：工作区可导入招标文件与补遗，投标文件中对其逐字应答的段落被识别并剔除；报告同时给出「原始相似度」与「剔除后相似度」，风险分级采用剔除后口径。
- **多家共有查证**：≥3 家共有的段落先查招标文件与内置范本库，查得到出处即豁免（置灰保留可复核），两库皆查不到才标为「待复核 · 涉嫌异常一致」。
- **分区分层阈值**：法定格式 / 报价 / 技术 / 商务四类章节按各自阈值判定，法定格式套话不再淹没真实雷同。

### 新增 · 商务标数值证据 Features
- **清单逐项比对**：识别工程量清单表并跨文档按编码对齐，给出逐项单价雷同率矩阵；检出「工程量 × 单价 ≠ 合价 且两份错得逐分一致」的共享算术错误。
- **报价规律性与相关性**：等差 / 等比 / 恒定折扣拟合、尾数聚集检验、单价向量相关性与归一化散点图（点全落对角线＝完全雷同，平行直线＝统一折扣）。
- **基准价敏感性**（描述性）：录入评标办法后重算「剔除嫌疑组后基准价如何移动、中标人是否翻转」。

### 新增 · 结论呈现 Features
- **证据强度口头等级**：围标结论以「强 / 中等 / 弱 支持同源编制」呈现，取代百分比分数。
- **复核路由三带**：条款按「低优先级抽查 / 需人工复核 / 重点标红」排队，低优先级只折叠不隐藏。
- **AI 复核建议**（默认关闭）：可选下载交叉编码器模型，为把握不足的条款给出复核倾向并据此排序；不改变分类，结论仍由人签发。
- **报告可引用性**：取证、规避、数值、区段、三带各节在 HTML / DOCX / Markdown / CSV / XLSX / JSON 六格式齐平；新增常驻的「检查方法与局限」章节，列明已执行的检查项与各自可被清除的方式。

### 修复 · 安全 Fixes
- 升级 lopdf 至 0.42、quick-xml 至 0.41，并连带升级 pdf-extract / calamine / plist，使依赖树中带漏洞的旧副本被完全移除（RUSTSEC-2026-0187 / -0194 / -0195；PDF 与 xlsx 均为投标人提供的不可信输入，仅升直接依赖并不够）；crossbeam-epoch 升至 0.9.20（RUSTSEC-2026-0204）。

### 修复 · 可复现性 Fixes
- **同输入同输出**：特征向量求和曾按哈希表迭代序做浮点累加，而浮点加法不满足结合律，导致同一份输入两次比对末位可能不同——改为定点累加，恢复逐字节可复现（可举证的前提）。

### 优化 · 工程 Engineering
- **校准语料与回归门禁**：内置合成对抗语料（1750 段对 + 12 组文档集，注入同源痕迹、规避字符、等比报价），CI 每次提交比对召回率 / 分类 F1 / 围标 AUC，指标漂移即失败——阈值改动从此有护栏。
- 数据库迁移 V13–V23，均为加表 / 加列，旧工作区打开即兼容。
- 全仓 `clippy --all-targets` 干净；语料哈希跨平台一致（此前 Windows 检出的换行转换会导致误报）。

### 文档 Docs
- 新增 `docs/bid-comparison-scheme.md`（比对方案与改进路线）、`docs/bid-comparison-sota-survey.md`（学界与工业界方案调研）、`docs/bid-comparison-execution-plan.md`（M0–M7 执行方案与设计明细）。
- `docs/license-activation-scheme-v2.md`：签发服务改为「通用授权内核、单产品部署」，每产品独立密钥对。

> ⚠️ **三带当前不做分流**：随包校准基于合成语料，而合成语料无法区分「同源编制」与「合法共享」（独立撰写的文档集里同样存在完全一致的范本段落），故所有条款一律路由到「需人工复核」并如实说明原因。补充真实脱敏语料后重跑校准即自动启用分流。
>
> ⚠️ **围标权重为实验性校准**：融合权重拟合自合成语料而非真实判例，报告中标注为实验性；经验权重保留为可回退路径。分级结论一律为线索级提示，认定权属评标委员会。
>
> ⚠️ **数值层覆盖范围**：仅支持 xlsx / docx / 文本 PDF 中可识别的清单表；扫描件 PDF 走 OCR 不产表格行，不在覆盖范围内。
>
> ℹ️ 归一化规则与解析产物有变更，首次打开既有工作区会重建分块缓存（一次性）。

## v0.5.0 — 2026-07-02

检测正确性强化（金额 / 日期 / 围标）+ 安全收口 + 失败重试与可达性；并含一轮深度架构分析与其修复。

### 新增 Features
- 失败 / 取消后「重试本次比对」一键复用原配置重跑，不再丢上下文。
- 批量导入模态焦点陷阱 + Esc 关闭；批注编辑器 Esc 取消 / Cmd·Ctrl+Enter 保存。

### 修复 · 检测正确性 Fixes
- **金额**：识别法定大写（壹佰万元）、千分位、¥ 前缀与小数；等额不同写法归一相等——事实冲突不再对最正式的大写报价条款失明，也不再因格式差异误判。
- **日期冲突**：粗细粒度兼容（2026年6月 与 2026年6月10日 不判冲突）；但缺年份不无条件通配、共有分量真不同仍判冲突。
- **围标**：共有特征词加罕见度过滤（通用模板词不再凑数）；报价梯度以「投标价」识别（排除注册资本 / 业绩 / 保证金劫持），表格式报价也能参与。
- **扫描件超页**：>20 页不再静默截断——显著提示「仅比对了前 N 页」并如实上报总页数。

### 修复 · 安全与稳定
- 最小 CSP（阻内联脚本执行）+ 导出扩展名校验，收窄「XSS → 任意写盘」放大链（opener scope 保留 `**` 以支持外部卷/网络盘导入导出，其安全权衡见 `SECURITY.md`）。
- 离线模型闸门按所选模型判定（禁联网时不误触下载）；任务终态守卫防 completed 被 cancel 覆写、并发不再建重复任务。

### 优化 · 性能 / 工程
- 查询精确失效（不再全量重取文档字节）、无限滚动移入 effect。
- 删除无消费者索引减写放大；CI 增 Windows/Linux 编译检查；macOS 保持 arm64（Apple 芯片）单架构（Intel 因 ort 无预编译库不支持）。

### 文档
- 新增 `docs/architecture-analysis-v0.4.md`（深度业务与架构分析）。

> ⚠️ 本版含较多检测行为变更（金额 / 日期 / 报价 / 围标），建议用真实标书端到端验证后再正式发布该草稿。

### 下载
- macOS（Apple 芯片 / arm64）：`BidGuard_0.5.0_aarch64.dmg`（Intel Mac 暂不支持，见 BUILD.md）
- Windows：`BidGuard_0.5.0_x64-setup.exe` 或 `BidGuard_0.5.0_x64_en-US.msi`
- Linux：`.AppImage`、`.deb`、`.rpm`

## v0.4.0 — 2026-07-01

界面深度打磨（可访问性 / 一致性 / 错误处理）+ 相似度打分与缓存显示修正。

### 修复 Fixes
- **相似度打分**：逐字相同的内容不再被结构维恒定压分（两份标书标题不同导致公共前缀恒为 0），现回到应有分数（可达 ~100%）；结构维仅在双方共享章节时才加分。
- **语义模型缓存**：工具箱显示的缓存体积不再因 HF 符号链接重复计数而翻倍。
- 关于区版本号硬编码、检查更新死代码、双击「开始比对」重复建任务、Markdown 锚点多实例串位、迷你矩阵空输入守卫等。

### 新增 / 改进 Features
- **可访问性**：全键盘可达 —— 开关 / 分段控件 / 侧栏导航 / 列表卡片 / 图标操作补齐 role 与 Enter/空格；Tab 焦点环随主色；Toast 可被屏幕阅读器朗读；批量导入模态 Esc 关闭。
- **错误与状态**：后端查询失败弹提示而非静默退化成空态；工具箱补加载 / 错误 / 重试态与环境自检重检；页面级错误显示中文兜底页 + 可复制错误详情。
- **防误操作**：删除模型 / 清空向量缓存 / 清理旧任务 / 删除批注均改两击确认，执行期禁用并切文案。
- **主题一致**：交互色统一跟随主色（换主色即时生效）；深色模式下原生控件、滚动条与画布外区域同步变暗；ghost 按钮悬停淡色底、不再像纯文字。
- **结果与预览**：相似度矩阵单元可点击下钻到逐对对比；相似度分档口径三屏统一；原文 txt/md/docx 定位一致；导出 CSV/JSON 说明与富文本格式区分；拖入文件时落区高亮。
- 任务搜索大小写不敏感；任务列表时间统一为相对时间 + 悬停显示精确时间。

### 下载
- macOS（Apple 芯片）：`BidGuard_0.4.0_aarch64.dmg`
- Windows：`BidGuard_0.4.0_x64-setup.exe` 或 `BidGuard_0.4.0_x64_en-US.msi`
- Linux：`.AppImage`、`.deb`、`.rpm`

## v0.3.0 — 2026-06-27

模型可见可管、查重源体系化、导出与结果屏去假数据。

### 新增 Features
- **工具箱**（侧栏新屏）：OCR 模型与语义模型分卡管理（状态/大小/下载/删除）、当前档「使用中」标记 +「设为当前」一键切换；存储（DB 体积 + VACUUM、清空向量缓存、清理旧任务）；环境自检（pdfium / OCR / 语义模型 / 数据库）。
- **查重源重构**：按分类折叠分组、搜索高亮、行内编辑、启用/停用、删除二次确认、批量导入（空行分段 / `分类|名称|正文` / CSV / JSON 四格式 + 预览去重）、每条样板「命中 N 份文档」统计。
- **OCR 可选档位**：升级 oar-ocr 0.7.1；PP-OCRv6 **tiny / small / medium** 三档可选（small 打包默认、medium 按需下载）；设置里选档、工具箱里管理。
- **语义模型可选**：注册表化（E5 / BGE），新增 **bge-large-zh-v1.5**、**multilingual-e5-large**，默认改 **bge-zh**；缓存按 model_id 隔离；暴露召回深度（candidateTopK）等设置。
- **解析增强**：docx 内嵌图片 OCR、PDF 段落回流、中英文句子切分；文档预览按分块类型标签/筛选 + 句子着色。

### 修复 Fixes
- **导出**：报告预览改真实数据；「包含正文全文 / 附配置快照」开关真正生效（per-export 覆盖）；去除硬编码假数据。
- **结果屏**：Matrix / Compare 去除假演示数据回落，改真空态；生产不再闪出虚构结果。
- **缓存检测**：修复语义模型经 HF-hub 符号链接缓存后仍显示「未下载」（walk_files 跟随符号链接）。
- **导入并发**：写入串行避免大文档并发 SQLITE_BUSY；启动清理卡在「解析中」的孤儿文档。
- **时间显示**：首页与任务列表按本地时区显示。

### 内部 Internal
- 结果屏原生消费 DTO，移除 useJobReport 过渡适配器（首屏从「summary + 全部 cluster/pair 详情」降为按需）。
- 声明 MSRV `rust-version = 1.95`（oar-ocr 0.7.1 要求）。
- 架构设计文档更新到当前实现态（§28.7–28.9）。

## v0.2.0

详见 GitHub Release v0.2.0。

## v0.1.0

首个版本。
